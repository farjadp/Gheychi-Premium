[build]
builder = "nixpacks"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[deploy.healthcheck]
# Admin panel healthcheck so Railway knows the service is up
path = "/"
timeoutSeconds = 30
